<div class="flyntComponent {{ options.theme }}" id="{{ preContentHtml|lower|replace({' ': ''})}}" is="flynt-block-cta">
  <div class="container">
    <div class="preContent">{{ preContentHtml|e('wp_kses_post') }}</div>
    <div class="content content-{{ options.textSize }}">
      <div class="h1Cta">{{ contentHtml|e('wp_kses_post') }}</div>
    </div>
    {% if ctaLink %}
    <a href="{{ ctaLink.url }}" class="cta-link">{{ ctaLink.title }}</a>
    {% endif %}
  </div>
</div>
