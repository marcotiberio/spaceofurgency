<div class="flyntComponent componentSpacing {{ options.theme }}" is="flynt-block-cta">
  <div class="container">
    <div class="content">
      <div class="h1Cta">{{ contentHtml|e('wp_kses_post') }}</div>
    </div>
    <a href="{{ ctaLink.url }}" class="cta-link">{{ ctaLink.title }}</a>
  </div>
</div>
